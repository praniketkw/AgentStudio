# Neon Postgres — get these from your Neon dashboard after creating a project
# https://console.neon.tech
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# NOTE: The Anthropic API key is NOT stored here.
# Users provide their own key in the UI — it is stored in their browser's localStorage only.
